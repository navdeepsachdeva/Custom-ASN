- name: Tower Machine Credentials Role Playbook
  hosts: localhost
  gather_facts: no

  tasks:
    # - name: Create Survey Job template via tower_job_template module
    #   ansible.controller.job_template:
    #     name: "ASN Template with Survey"
    #     description: "Fantstic Via Playbook"
    #     organization: "asn"
    #     job_type: "run"
    #     inventory: "bank"
    #     project: "GIT ASN Project"
    #     playbook: "survey_demo.yml"
    #     credentials:
    #       - "Credential Servers"
    #     become_enabled: "true"
    #     survey_spec: "{{ lookup('ansible.builtin.file','./mysurvey.yml') | from_yaml }}"
    #     survey_enabled: true
    #     state: "present"
    #     controller_config_file: "./controller_cli.cfg"

    - name: Launch a workflow
      ansible.controller.workflow_launch:
        workflow_template: "Second Complete Flow"
        controller_config_file: "./controller_cli.cfg"
      run_once: true
      register: workflow_results


#### This Task will provide final job status

    - name: Print job status
      ansible.builtin.debug:
        msg: "{{ workflow_results }}"
        # msg: " Status = {{ workflow_results.job_info.status }}  Started Time = {{ workflow_results.job_info.started }}   Finished Time = {{ workflow_results.job_info.finished }}      Time Taken = {{ workflow_results.job_info.elapsed }} Seconds"


  # - name: Create Job template via tower_job_template module
  #   ansible.controller.job_template:
  #     name: "Database setup"
  #     description: "Setup database servers"
  #     job_type: "run"
  #     inventory: "DB Inventory"
  #     project: "DB Project"
  #     playbook: "create_samples_db.yml"
  #     credentials:
  #       - "Developers"
  #     # become_enabled: "true"
  #     state: "present"
  #     controller_config_file: "./controller_cli.cfg"
  #   register: job_template_output
  # 
  # - name: Print job_template_output
  #   debug:
  #     var: job_template_output

  # - name: Assign permissions (role) to "Database setup" template
  #   ansible.controller.role:
  #     job_template: "Database setup"
  #     team: "Developers"
  #     role: "admin"
  #     state: "present"
  #     controller_config_file: "./controller_cli.cfg"





  # - name: Launch Job {{ job_template_info.name }}
  #   ansible.controller.job_launch:
  #     job_template: "Database setup"
  #     controller_host: https://controller.lab.example.com
  #     controller_password: redhat123
  #     controller_username: daniel
  #     validate_certs: false
  #     controller_config_file: "./controller_cli.cfg"
  #   register: job_output
  #  
  # ####   Check Job ID and other information  
  #  
  # - name: Print job status
  #   debug:
  #     var: job_output
  #  
  # #### Wait and Monitor status of previous launched job
  #  
  # - name: Wait for job max 120s
  #   ansible.controller.job_wait:
  #     job_id: "{{ job_output.id }}"
  #     timeout: 120
  #     controller_config_file: "./controller_cli.cfg"
  #   register: check_status
  #  
  # ####   Check Final Status of Job and Time taken to execute 
  #  
  # - name: Job Status Information
  #   ansible.builtin.debug:
  #     msg: "Time Taken = {{ check_status.elapsed }}    Job ID = {{ check_status.id }}   Job Status = {{ check_status.status }}"
